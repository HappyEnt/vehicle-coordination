version: "3.9"
services:
  ros:
    build: ./
    volumes:
      - ./:/ros
      - /tmp/shared:/shared
    # add environment variable
    network_mode: host
    environment:
      - WEBOTS_SHARED_FOLDER=/tmp/shared:/shared
